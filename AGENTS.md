# Agent instructions

- Treat Markdown in the configured vault as canonical.
- Never copy a user vault into the public application repository.
- Use only fictional examples in tests and documentation.
- Do not mark tasks complete without explicit evidence.
- Preserve unknown frontmatter fields.
- Keep edits constrained to `PM_VAULT_ROOT`.
- Run backend tests, frontend tests, the production build, and the privacy scan
  before committing.
- Never print or commit app tokens, GitHub tokens, private paths, or vault
  excerpts in logs and fixtures.
